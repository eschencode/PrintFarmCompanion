# Generate a random token for AUTH_TOKEN
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_TOKEN=your_generated_token_here

# Server configuration (defaults shown, uncomment to override)
# PORT=3001
# HOST=127.0.0.1